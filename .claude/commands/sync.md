---
description: Pull the latest code into the dev folder
---

Run `git pull --ff-only` in this dev folder.

Report what changed since her last pull in plain language — which files, and what
the change was for — or say "nothing new" if it was already current.

If the pull is refused, it means there are uncommitted edits here or the history
has diverged. Explain what is in the way and offer to sort it out. Do not force
anything.
