---
description: Start the job search running again (counterpart to /pause)
---

Delete `~/Meester/PAUSED` and confirm it is gone.

Then offer to run `bash ~/Meester/scripts/run_harvest.sh` so she doesn't have to
wait up to an hour for the next scheduled cycle — and if she says yes, show her
the result in plain language rather than raw log output.

---

*Named `/unpause`, not `/resume`: in this repo "resume" means her CV, and the
pre-push guard blocks that filename on sight. The guard is correct — the command
name was the thing worth changing.*
